# Security

IAM Playground is a lab only. Do not point it at production tenants.

Do not commit `.env`, passwords, tokens, authorization codes, or model keys. Do not commit bearer tokens, client secrets, or cookies.

Report vulnerabilities later via a private advisory once the GitHub repository exists. Until then, the repository is local.

Loopback binding is not the whole security model.
