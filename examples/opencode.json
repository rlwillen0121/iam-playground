{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "orchestrate",
  "agent": {
    "orchestrate": {
      "description": "IAM Playground contributor coordinator. No source edits and no lab runs. This is the playground, not Okta or Entra.",
      "mode": "primary",
      "permission": {
        "edit": "deny",
        "task": {
          "*": "deny",
          "implement": "allow",
          "review": "allow"
        }
      }
    },
    "implement": {
      "description": "IAM Playground implementer. Edits named files and does not run the lab. This is the playground, not Okta or Entra.",
      "mode": "subagent",
      "permission": {
        "edit": "allow",
        "task": "deny"
      }
    },
    "review": {
      "description": "IAM Playground review composer. Hidden lanes only, no edits. This is the playground, not Okta or Entra.",
      "mode": "subagent",
      "permission": {
        "edit": "deny",
        "task": {
          "*": "deny",
          "lane-rules": "allow",
          "lane-security": "allow",
          "lane-completeness": "allow",
          "lane-eng": "allow",
          "lane-lab": "allow"
        }
      }
    },
    "lab-operator": {
      "description": "IAM Playground operator. Local lab only, no repo edits. This is the playground, not Okta or Entra.",
      "mode": "primary",
      "permission": {
        "edit": "deny",
        "task": "deny"
      }
    }
  }
}
